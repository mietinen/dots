#!/usr/bin/env bash

if [ "$#" -lt 1 ]; then
    echo -en "\x00prompt\x1fSelect man page\n"
    man -k . | awk '{ print $1 }'
else
    tempfile=$(mktemp)
    man -Tpdf "$*" > "$tempfile"
    coproc {
        zathura "$tempfile"
        rm -f "$tempfile"
    } >/dev/null 2>&1
fi
